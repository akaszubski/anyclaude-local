# LMStudio Configuration
# Copy this file to .env and customize the values

# LMStudio server URL (default: http://localhost:1234/v1)
LMSTUDIO_URL=http://localhost:1234/v1

# Specific model to use (optional - auto-detects if not set)
# When not set, anyclaude will automatically use whatever model is loaded in LMStudio
# Uncomment and set if you want to force a specific model:
# LMSTUDIO_MODEL=qwen2.5-vl-7b-instruct

# LMStudio API key (default: lm-studio)
LMSTUDIO_API_KEY=lm-studio

# Debug logging (optional)
# 1 = basic debug info, 2 = verbose debug with full request/response bodies
# ANYCLAUDE_DEBUG=1

# Proxy-only mode (optional)
# Set to "true" to run the proxy server without spawning Claude Code
# Useful for testing or debugging
# PROXY_ONLY=true
