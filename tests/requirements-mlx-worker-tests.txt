# MLX Worker Test Dependencies

# Core testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# FastAPI testing
httpx>=0.24.0
fastapi>=0.100.0
uvicorn>=0.23.0

# MLX (for mocking)
# Note: These are mocked in tests, but may need stubs
# mlx-lm>=0.0.1

# Utilities
pydantic>=2.0.0
